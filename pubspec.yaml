name: kola
description: Bloger helper engine

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_sheet_localization: ^5.1.1
  get: ^4.1.4
  cupertino_icons: ^1.0.3
  shared_preferences: ^2.0.6
  dio: ^4.0.0
  flutter_svg: ^0.22.0
  toast: ^0.1.5
  url_launcher: ^6.0.9
  json_annotation: ^4.0.1
  google_fonts: ^2.1.0
  getwidget: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  json_serializable: ^4.1.3
  flutter_native_splash: ^1.2.0

flutter:
  uses-material-design: true
